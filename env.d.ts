// Fix: Use `path` for URL-based type definitions. `types` is for package names, which do not resolve URLs.
// FIX: Switched CDN from esm.sh to unpkg.com to resolve "File not found" errors.
/// <reference path="https://unpkg.com/@types/react@18.3.1/index.d.ts" />
// Fix: Corrected the version of @types/react-dom from 18.3.1 to 18.3.0, as version 18.3.1 does not exist.
// Fix: Use `path` for URL-based type definitions. `types` is for package names, which do not resolve URLs.
// FIX: Switched CDN from esm.sh to unpkg.com to resolve "File not found" errors.
/// <reference path="https://unpkg.com/@types/react-dom@18.3.0/index.d.ts" />